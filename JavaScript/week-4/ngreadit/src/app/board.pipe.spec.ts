/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { BoardPipe } from './board.pipe';

describe('BoardPipe', () => {
  it('create an instance', () => {
    const pipe = new BoardPipe();
    expect(pipe).toBeTruthy();
  });
});
